<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bekamapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Vendor.VendorInfoActivity"/>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".Vendor.AddProductActivity" />
        <activity android:name=".Vendor.MyProductsActivity" />
        <activity android:name=".Vendor.MyProfileActivityVendor" />
        <activity android:name=".User.MyProfileActivityUser" />
        <activity android:name=".User.ItemDetailsActivity" />
        <activity android:name=".ReviewsActivity"></activity>
    </application>

</manifest>